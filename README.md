# 🎫 HelpDesk — IT Support Ticket Tracker

Система учёта заявок в IT-поддержку, реализованная в виде микросервисной архитектуры на .NET 8.

## 📋 О проекте

HelpDesk позволяет регистрировать обращения пользователей в службу поддержки, отслеживать их статусы, назначать ответственных инженеров и контролировать качество сервиса.

Система разработана как проект с упором на микросервисную архитектуру и асинхронное взаимодействие через брокер сообщений.

---

## 🏗️ Архитектура

Проект разделён на три независимых микросервиса и один общий контрактный проект:

```
HelpDesk/
├── Shared/
│   └── HelpDesk.Contracts        # Общие контракты, DTO, сообщения RabbitMQ
│
└── src/
    └── Services/
        ├── User/                  # Сервис сотрудников
        ├── Category/              # Сервис категорий
        └── ProblematicApplication/ # Сервис заявок (основной)
```

Каждый микросервис реализован по принципу **Clean Architecture** и включает 4 слоя:
- **Domain** — сущности, бизнес-правила, интерфейсы
- **Application** — use-cases, обработчики команд/запросов
- **Infrastructure** — работа с БД, RabbitMQ, внешние зависимости
- **Presentation** — контроллеры, Web API

Сервисы **не знают напрямую друг о друге** — взаимодействие происходит исключительно через события в RabbitMQ.

---

## ⚙️ Технологический стек

| Категория | Технология |
|-----------|-----------|
| Платформа | .NET 8, ASP.NET Core Web API |
| ORM | Entity Framework Core |
| База данных | PostgreSQL |
| Брокер сообщений | RabbitMQ |
| Документация API | Swagger |
| Контейнеризация | Docker, Docker Compose |
| Архитектурный стиль | Микросервисы, DDD, Clean Architecture |

---

## 📦 Основные сущности

- **Сотрудник (User)** — пользователи системы с ролями: Пользователь, Инженер 1-й линии, Инженер 2-й линии, Руководитель
- **Заявка (ProblematicApplication)** — обращение в IT-поддержку с приоритетом, статусом, SLA-дедлайном, наблюдателями
- **Категория (Category)** — тип обращения (Инцидент, Запрос на обслуживание, Доступы и т.д.)

---

## 🔄 Жизненный цикл заявки

```
Новая → Назначена инженеру → В работе → Решена → Закрыта
                                      ↘ Ожидает информации → В работе
         ↘ Отменена (из любого начального/промежуточного статуса)
```

Переходы строго детерминированы. При не соблюдения порядка выскакивает исключение

---

## 📨 Асинхронные события (RabbitMQ)

Сервисы публикуют события при ключевых действиях:
- `ApplicationCreatedMessage` — заявка создана
- `ApplicationStatusChangedMessage` — статус заявки изменён
